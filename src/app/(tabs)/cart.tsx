import { SafeScreen } from "@/components";
import { Text } from "react-native";

const Cart = () => {
  return (
    <SafeScreen>
      <Text className="text-2xl font-bold text-center">Cart</Text>
    </SafeScreen>
  );
};

export default Cart;
