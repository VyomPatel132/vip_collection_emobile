import { SafeScreen } from "@/components";
import { Text } from "react-native";

const Addresses = () => {
  return (
    <SafeScreen>
      <Text>addresses</Text>
    </SafeScreen>
  );
};

export default Addresses;
