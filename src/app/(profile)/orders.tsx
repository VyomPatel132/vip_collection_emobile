import { SafeScreen } from "@/components";
import { Text } from "react-native";

const Orders = () => {
  return (
    <SafeScreen>
      <Text>Orders</Text>
    </SafeScreen>
  );
};

export default Orders;
